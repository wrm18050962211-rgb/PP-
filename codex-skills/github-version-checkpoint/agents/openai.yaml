interface:
  display_name: "GitHub Version Checkpoint"
  short_description: "Verify, commit, and push GitHub checkpoints."
  default_prompt: "Use $github-version-checkpoint to verify changes, commit them, and push a recoverable version to GitHub."

policy:
  allow_implicit_invocation: true
