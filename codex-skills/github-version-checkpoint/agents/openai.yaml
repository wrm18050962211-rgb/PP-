interface:
  display_name: "GitHub Version Checkpoint"
  short_description: "Save local checkpoints, then push good versions."
  default_prompt: "Use $github-version-checkpoint to save local Git checkpoints for VS Code review, then push to GitHub when I approve."

policy:
  allow_implicit_invocation: true
